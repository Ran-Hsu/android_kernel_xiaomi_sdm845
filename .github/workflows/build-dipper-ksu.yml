name: Build dipper KernelSU

on:
  workflow_dispatch:

concurrency:
  group: dipper-ksu-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: dipper KernelSU v0.9.5
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    env:
      ARCH: arm64
      SUBARCH: arm64
      KBUILD_BUILD_USER: dipper
      KBUILD_BUILD_HOST: github-actions
      LLVM_IAS: "1"
    steps:
      - name: Checkout kernel
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            bc bison flex libssl-dev libelf-dev libncurses-dev \
            python3 python-is-python3 zip unzip wget git ccache \
            device-tree-compiler clang lld llvm \
            gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi \
            binutils-aarch64-linux-gnu binutils-arm-linux-gnueabi \
            build-essential ca-certificates
          which clang aarch64-linux-gnu-as arm-linux-gnueabi-as \
            aarch64-linux-gnu-ld arm-linux-gnueabi-ld
          clang --version
          arm-linux-gnueabi-as --version | head -n 1

      - name: Setup KernelSU v0.9.5
        run: |
          set -euo pipefail
          rm -f drivers/kernelsu
          if grep -q 'obj-y += kernelsu/' drivers/Makefile; then
            sed -i '/obj-y += kernelsu\//d' drivers/Makefile
          fi
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/v0.9.5/kernel/setup.sh" | bash -s v0.9.5
          ls -l drivers/kernelsu KernelSU/kernel
          grep -n kernelsu drivers/Makefile drivers/Kconfig
          if ! grep -q '^CONFIG_KSU=y' arch/arm64/configs/vendor/xiaomi/mi845_defconfig; then
            printf '\nCONFIG_KSU=y\n' >> arch/arm64/configs/vendor/xiaomi/mi845_defconfig
          fi
          if ! grep -q '^CONFIG_OVERLAY_FS=y' arch/arm64/configs/vendor/xiaomi/mi845_defconfig; then
            printf 'CONFIG_OVERLAY_FS=y\n' >> arch/arm64/configs/vendor/xiaomi/mi845_defconfig
          fi
          sed -i 's|--prefix=$(GCC_TOOLCHAIN32_DIR)|--prefix=$(GCC_TOOLCHAIN32_DIR)$(notdir $(CROSS_COMPILE_ARM32))|' arch/arm64/Makefile

      - name: Fix KernelSU C89 syscall hooks
        run: |
          python3 - <<'PY'
          import re
          from pathlib import Path

          def must_sub(path, pattern, repl, label):
              text = Path(path).read_text()
              new, n = re.subn(pattern, repl, text, count=1)
              if n != 1:
                  raise SystemExit(f"{label}: expected 1 match, got {n}")
              Path(path).write_text(new)
              print(f"patched {path}")

          must_sub(
              "fs/open.c",
              r"(SYSCALL_DEFINE3\(faccessat, int, dfd, const char __user \*, filename, int, mode\)\n\{\n)[ \t]*ksu_handle_faccessat\(&dfd, &filename, &mode, NULL\);\n((?:[ \t]+[^\n]+\n)+)",
              r"\1\2\n\tksu_handle_faccessat(&dfd, &filename, &mode, NULL);\n",
              "open.c faccessat",
          )
          must_sub(
              "fs/stat.c",
              r"(int vfs_fstatat\(int dfd, const char __user \*filename, struct kstat \*stat,\n[ \t]*int flag\)\n\{\n)[ \t]*ksu_handle_stat\(&dfd, &filename, &flags\);\n((?:[ \t]+[^\n]+\n)+)",
              r"\1\2\n\tksu_handle_stat(&dfd, &filename, &flag);\n",
              "stat.c vfs_fstatat",
          )
          PY

      - name: Merge dipper defconfig
        run: |
          set -euo pipefail
          mkdir -p out
          cat arch/arm64/configs/vendor/xiaomi/mi845_defconfig \
              arch/arm64/configs/vendor/xiaomi/dipper.config \
              > arch/arm64/configs/dipper_ksu_defconfig
          make O=out ARCH=arm64 LLVM_IAS=1 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            dipper_ksu_defconfig
          grep -E 'CONFIG_KSU|CONFIG_OVERLAY_FS|CONFIG_MACH_XIAOMI_E1N|CONFIG_COMPAT_VDSO' out/.config

      - name: Build kernel
        run: |
          set -euo pipefail
          make O=out ARCH=arm64 LLVM_IAS=1 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip LD=ld.lld \
            -j"$(nproc)"
          test -f out/arch/arm64/boot/Image.gz-dtb
          ls -lh out/arch/arm64/boot/Image.gz-dtb

      - name: Pack AnyKernel3
        run: |
          set -euo pipefail
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git ak3
          rm -rf ak3/.git ak3/.github
          cat > ak3/anykernel.sh <<'EOF'
          # AnyKernel3 Ramdisk Mod Script
          properties() { '
          kernel.string=dipper LineageOS KernelSU v0.9.5
          do.devicecheck=1
          do.modules=0
          do.systemless=1
          do.cleanup=1
          do.cleanuponabort=0
          device.name1=dipper
          device.name2=MI 8
          supported.versions=
          supported.patchlevels=
          supported.vendorpatchlevels=
          '; }
          block=boot
          is_slot_device=auto
          ramdisk_compression=auto
          patch_vbmeta_flag=auto
          . tools/ak3-core.sh
          dump_boot
          write_boot
          EOF
          sed -i 's/^          //' ak3/anykernel.sh
          cp out/arch/arm64/boot/Image.gz-dtb ak3/
          mkdir -p artifacts
          (cd ak3 && zip -r9 ../artifacts/dipper-lineage-ksu-v0.9.5.zip .)
          cp out/arch/arm64/boot/Image.gz-dtb artifacts/
          ls -lh artifacts

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dipper-ksu-v0.9.5
          path: artifacts/*
          if-no-files-found: error
          retention-days: 14
